public class TiaoJianYunSuan {
    // variable x = (expression) ? value if true : value if false
    // 条件运算符 ？： 若（）内值为真 返回：前的值，否则返回：后的值
    public void putresult1(int a){
        String b = (a == 1) ? "true" : "false";
        System.out.println("the result of () is : " + b);
    }

//    用于检查对象是否是某一特定类型
//boolean result = args[0] instanceof String;


}
